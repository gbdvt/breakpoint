# breakpoint

## Desktop (Tauri)

Premium UI also lives in **`desktop/`** (Vite + React + Tauri 2). From repo root:

```bash
cd desktop
npm install
npm run tauri:dev
```

Main window = dashboard; use the sidebar to open the **frameless, always-on-top floating session** webview.
